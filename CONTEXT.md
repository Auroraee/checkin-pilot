# 自动签到

本上下文描述用户在多个 AI API 面板站点上领取每日签到奖励的共同语言，并区分站点身份、平台兼容性与签到结果。

## Language

**站点（Site）**:
由一个 origin 标识的独立面板部署；同一平台类型的不同站点仍可能拥有不同规则和私有改动。
_Avoid_: 平台、接口

**平台类型（Platform Family）**:
站点所基于的上游面板家族，例如 New API 或 Sub2API；它只表示兼容性线索，不保证站点具有某项能力。
_Avoid_: 站点类型、网站源码

**站点能力（Site Capability）**:
一个具体站点经探测或真实响应确认支持的签到行为。站点能力优先于平台类型的默认推断。
_Avoid_: 平台功能、猜测兼容

**已验证适配（Verified Integration）**:
已经针对命名站点的真实协议完成契约验证，并纳入正式验收范围的适配。
_Avoid_: 理论支持、可能兼容

**探测兼容（Detected Compatibility）**:
运行时能力探测通过并可尝试使用通用适配器的状态；它不是对该站长期兼容性的正式保证。
_Avoid_: 官方支持、已验证适配

**已授权站点（Authorized Site）**:
用户主动加入并明确允许工具访问的站点。发现或访问过某个站点并不自动使其成为已授权站点。
_Avoid_: 已发现站点、所有网站

**站点登记（Site Enrollment）**:
用户在已登录的当前站点上授予 origin 权限、完成能力探测并确认启用，从而使其成为已授权站点的过程。
_Avoid_: 自动发现、粘贴即绑定

**暂停自动签到（Automation Paused）**:
已授权站点不参加计划签到与当日补跑的状态；它不撤销授权或会话绑定，用户仍可显式发起单站手动签到。
_Avoid_: 禁用站点、退出登录

**移除站点（Site Removal）**:
经用户再次确认后，终止站点全部待处理工作并删除其授权、绑定、配置与签到记录的不可恢复操作；仅防止同日重加绕过计算上限的安全预算墓碑保留至调度日结束。
_Avoid_: 暂停、退出登录

**签到账户（Check-in Account）**:
用户在一个站点上的独立身份，也是每日签到状态与奖励归属的主体。
_Avoid_: API Key、浏览器账号

**会话绑定（Session Binding）**:
一个已授权站点与当前浏览器配置中单一签到账户的关联；关联在站点登录失效时失效，不包含被工具复制保存的认证秘密。
_Avoid_: 保存账号、Token 绑定

**账号重绑（Account Rebinding）**:
站点当前登录身份变化后，由用户显式确认以新的签到账户替换原会话绑定的过程；身份变化本身不会自动触发重绑。
_Avoid_: 自动换号、会话刷新

**绑定代次（Binding Generation）**:
同一站点一次连续会话绑定期的匿名标识。账号重绑会开始新代次，旧代次记录保留至到期但不与当前账号汇总。
_Avoid_: 多账号、用户档案

**调度日（Schedule Day）**:
由 Chrome 所在设备的本地日历日期划分的自动化周期；一个调度日只有一个全局签到批次，并拥有独立的补跑、重试和计算预算。
_Avoid_: 签到日、自然日

**站点服务日（Site Service Day）**:
目标站点判定每日签到资格的日期边界；工具不推测其时区，只把站点返回的签到状态作为资格依据。
_Avoid_: 调度日、浏览器日期

**当日补跑（Same-day Catch-up）**:
计划机会错过后，在同一调度日内发起的签到尝试；本地日期变化后，旧调度日不再补跑。
_Avoid_: 补签、跨日补偿

**签到窗口（Check-in Window）**:
一个调度日内允许全局签到批次发生的时间区间，实际执行时刻从区间内选择，不承诺精确到某一分钟。
_Avoid_: 签到时间、精确闹钟

**签到尝试（Check-in Attempt）**:
针对一个签到账户在一个调度日发起的一次领取意图，其结果可以是领取成功、已经领取、需要用户介入或失败。
_Avoid_: 请求、定时任务

**签到批次（Check-in Batch）**:
在同一调度机会中到期的一组签到尝试；各尝试结果彼此独立，一个账号需要用户介入不阻塞其他账号。
_Avoid_: 并发请求、全局任务

**可重试失败（Retryable Failure）**:
预期可能在同一调度日内自行恢复，且在重新查询站点签到状态后可安全再次尝试的失败。
_Avoid_: 所有错误、业务拒绝

**终止结果（Terminal Outcome）**:
结束该次自动流程且不应继续自动重试的结果，包括完成、需要用户介入、不兼容和明确业务拒绝。
_Avoid_: 永久错误、异常

**签到记录（Check-in Record）**:
一次签到尝试留下的脱敏结果证据，用于让用户确认发生了什么；它不是原始网络请求或认证资料的副本。
_Avoid_: 请求日志、运行遥测

**安全挑战（Security Challenge）**:
站点在接受签到前要求完成的附加证明，可以是工具明确支持的有限计算，也可以是必须由用户完成的交互验证。
_Avoid_: 验证码、风控失败

**计算型挑战（Computational Challenge）**:
协议已被明确识别、计算量有界且可随时取消的安全挑战，例如站点规定的签到 PoW。
_Avoid_: 挖矿、自动验证码

**需要用户介入（User Action Required）**:
签到无法静默完成，必须由用户恢复登录或完成人机验证后才能继续的结果。
_Avoid_: 自动绕过、普通失败
