---
status: accepted
---

# 所有数据留在浏览器本地且不做遥测

项目不设后端、不上传使用统计，也不收集遥测。配置与签到记录仅保存在本地；记录保留 30 天且每站最多 100 条，只含时间、origin、归一化结果、奖励、耗时、重试次数和脱敏错误摘要，绝不保存请求头、Cookie、Token、密码或原始响应正文。用户确认移除站点时，其全部绑定代次、配置和记录立即删除；唯一例外是防止同日重加绕过 PoW 上限的安全预算墓碑，它只含 origin、调度日和已用次数/秒数，并在本地午夜自动删除。该边界放弃集中式故障分析，以换取可验证的最小数据暴露面。
