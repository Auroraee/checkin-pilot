---
status: accepted
---

# 每个站点都必须由用户显式授权

扩展只处理用户通过站点登记主动加入的 origin，并在加入时请求该 origin 的可选主机权限。项目不扫描所有网页、不把浏览历史当作站点来源，也不申请 `<all_urls>`；移除站点时取消其待处理工作并撤销不再需要的 origin 权限。为防止同日移除重加绕过 PoW 上限，已用预算的最小安全墓碑可保留到本地调度日结束，但它不授予网络访问权限。这牺牲了零配置自动发现，以换取更小的数据访问面、更可信的源码公开分发和更清晰的 Chrome 权限说明。
