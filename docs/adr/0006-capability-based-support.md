---
status: accepted
---

# 兼容承诺以已验证能力为准而不是平台名称

首版将 `runanytime.hxi.me` 作为已验证适配，并允许现有浏览器会话与标准签到接口探测成功的 New API 站点以“探测兼容”方式运行；依赖长期面板 Token 的鉴权明确不支持。Sub2API 只做平台识别，不宣称通用签到能力，具有私有签到功能的部署必须新增站点适配器后才能成为已验证适配。

